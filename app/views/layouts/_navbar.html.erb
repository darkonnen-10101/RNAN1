<nav class="navbar fixed-top navbar-expand-lg navbar-inverse">
  <a class="navbar-brand" href="#">Darkonnen</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if request.fullpath == personas_path %>
        <li class="nav-item">
          <a class="nav-link" href="<%= proyectos_path %>">Proyectos</a>
        </li>
      <% else %>
        <li class="nav-item">
          <a class="nav-link" href="<%= personas_path %>">Personas</a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
