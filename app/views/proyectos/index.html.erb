<p id="notice"><%= notice %></p>

<h1>Proyectos</h1>

<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @proyectos.each do |proyecto| %>
      <tr>
        <td><%= proyecto.title %></td>
        <td><% proyecto.personas.each do |persona| %>
        <%= persona.name %>
        <a href="<%= remove_person_proyecto_path(proyecto, persona) %>" data-method="delete">
          <span class="badge">X</span></a>
      <% end %>
      <%= form_tag(add_person_proyecto_path(proyecto)) do %>
      <%= text_field_tag "title" %>
    <% end %>
</td>

        <td><%= link_to 'Mostrar Team', proyecto %></td>
        <td><%= link_to 'Editar título', edit_proyecto_path(proyecto) %></td>
        <td><%= link_to 'Eliminar proyecto', proyecto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Proyecto', new_proyecto_path %>
